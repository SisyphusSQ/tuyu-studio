# Bad Project Fixtures

These fixtures are intentionally invalid or incomplete. They are small,
sanitized examples for recovery and Health Check tests.

- `invalid-json`: broken `project.tuyu.json` for manifest recovery copy.
- `missing-references`: valid manifest with broken graph/package/digest references.

Do not add real user paths, complete URLs, credentials, tokens, or media files
to this directory.
